version: '3'
services:
  api:
    build:
      context: ./server
    volumes:
      - /app/node_modules
      - ./server:/app
    expose:
      - 80
    environment:
      VIRTUAL_HOST: hcaptcha.wmtech.cc
      LETSENCRYPT_HOST: hcaptcha.wmtech.cc
      LETSENCRYPT_EMAIL: swmeng@yes.my
      PORT: 80

networks:
    default:
        external:
            name: nginx-proxy
